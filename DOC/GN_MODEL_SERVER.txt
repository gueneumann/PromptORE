GN, 02.09.2026:

Verwende nvtop zur Visualisierung der GPU Auslastung


unterscheide lokale Modelle  und remote Modellen:

- für remoter Modelle:
    vLLM.ai framework benutzen

- ore_models code so anpassen, das entweder lokales model
    oder remote model verwendet werden kann

Verwendung vllm.ai docker file:

MLT GPU Server

ssh serv-9210.kl.dfki.de -l neumann

P: MQj*******, DG4A4


VLLM.AI Framework: OpenAI API

Docker starten:

cd /raid/data/neumann/vllm

VLLM docker starten im Hintergrund:

docker compose up -d

Log anschauen:

docker compose logs -f

Hier Modelle gecached für Wiederverwendung, wenn docker neu gestart wird

/raid/data/neumann

docker beenden: docker compose down

Editiere docker-compose.yaml

Client call: für Batch, list of sentences
curl http://serv-9210.kl.dfki.de:8080/v1/embeddings \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Qwen/Qwen3-Embedding-4B",
    "input": "What is the capital of France?"
  }'

