TODO  Why should I compute labels? for knowledge graph construction

- how to predict class label from predicted label?
    compute_cluster_label_name.py

- choosing just tokens between H and T has label source is not sufficient and often returns []
    - if for (H, T) == [] then get string between (T, H) -> DONE
    - what are the remaining [] cases?

- major problem so far: the generated clusters are highly ambiguous
    - they usually contain instances from different "gold" clusters/labels
    - so, it looks like one would need to split and reshuffle the instances
        - e.g., create clusters with "save" instances -> compute a ranking based on topic words
        - collect "unsave" instances -> maybe do KNN on this item set
        - check V-measure between clusterings
    - if I can predict labels, can I use them for labeling instances and then do classification?

- class distribution
    - assuming I can pre-classify NORELs, different distribution of labels
        still an issue for clustering
    - how can one use info about unbalanced label distribution?
        is it possible to recognize this via label prediction?

- compute distribution of labels per cluster
    -> the heat map is already doing this

TODO use BertTopic for labeling?

TODO What is the task of labeling ?
- given item set D, partition it into k groups
    - somehow it means we assign a lebel r to each item
- often K is given, as for KNN


- what does it mean if K is not given?
  - depends on task and how input is structured, e.g., (H,T) entities are given for relation extraction
  - but the general goal is mostly to identify groups of related items
  - so K should be between 1 and |D| and "reasonable" large
  - but Elbow rule can predict k (but very expensive)
    - maybe provide min/max borders?

what does NOREL mean here?
- I guess only, relevant for evaluation



